<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="com.ssafy.mapper.houseinfo">
  	<select id="search" parameterType="map" resultType="houseinfo">
  		select * from houseinfo where dong=#{dong} and AptName=#{name}
  	</select>
</mapper>